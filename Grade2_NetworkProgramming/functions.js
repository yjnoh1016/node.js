// function 작성방법
function add1(x, y) {
  return x + y;
}

// function 대신 화살표 함수 사용
const add2 = (x, y) => {
  return x + y;
};

const add3 = (x, y) => x + y;

const add4 = (x, y) => x + y;

//매개변수가 1개인 경우 괄호 생략 가능함.
function not1(x) {
  return !x;
}

const not2 = (x) => !x;
